{
  "name": "homebase",
  "version": "1.0.0",
  "private": true,
  "description": "Config-driven, SEO-optimized personal landing page template powered by Eleventy.",
  "license": "MIT",
  "scripts": {
    "build": "eleventy --input=src --output=_site",
    "start": "eleventy --input=src --output=_site --serve",
    "clean": "npx rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@napi-rs/canvas": "^0.1.68",
    "canvas": "^3.2.1",
    "js-yaml": "^4.1.0",
    "qrcode": "^1.5.4",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
